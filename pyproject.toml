[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "n3xuslib"
version = "0.1.0"
description = "Shared daemon library — write events to n3xusDB event_outbox"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "asyncpg>=0.30",
]
